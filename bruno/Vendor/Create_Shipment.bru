meta {
  name: "Create Shipment"
  type: "http"
  seq: 14
}

post {
  url: "{{base_url}}/vendor/orders/{{order_id}}/fulfillments/{{fulfillment_id}}/shipments"
}

headers {
  Content-Type: "application/json"
  x-publishable-api-key: "{{publishable_key}}"
  authorization: "Bearer {{vendor_token}}"
}

body:json {
  "items": [
    {
      "id": "{{line_item_id}}",
      "quantity": 1
    }
  ],
  "labels": []
}
