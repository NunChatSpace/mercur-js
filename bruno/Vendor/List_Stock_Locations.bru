meta {
  name: "List Stock Locations"
  type: "http"
  seq: 12
}

get {
  url: "{{base_url}}/vendor/stock-locations"
}

headers {
  x-publishable-api-key: "{{publishable_key}}"
  authorization: "Bearer {{vendor_token}}"
}
